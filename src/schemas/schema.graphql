extend input DiscountConditionsInput {
  "Discount conditions input type"
  variants: [String]
}

extend type DiscountConditions {
  "Variant that may apply this discount code"
  variants: [String]
}

extend enum DiscountCalculationMethod {
  "Variant specific discount"
  variant
}